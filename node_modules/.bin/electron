#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_electron-prebuilt@1.4.13@electron-prebuilt/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../_electron-prebuilt@1.4.13@electron-prebuilt/cli.js" "$@"
  ret=$?
fi
exit $ret
