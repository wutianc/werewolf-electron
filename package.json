{
  "name": "Werewolf",
  "version": "1.0.0",
  "description": "狼人杀门店系统",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "Win64": "electron-packager . Werewolf --platform=win32 --arch=x64 --out=./outapp --overwrite --app-version=1.0.0 --icon=./static/logo.ico",
    "Win32": "electron-packager . Werewolf --platform=win32 --arch=ia32 --out=./outapp --overwrite --app-version=1.0.0 --icon=./static/logo.ico",
    "running": "electron ."
  },
  "keywords": [],
  "author": "Webb.W",
  "license": "ISC",
  "dependencies": {
    "electron-packager": "^9.1.0",
    "electron-prebuilt": "^1.4.13",
    "jquery": "^3.2.1"
  },
  "devDependencies": {
    "electron-packager": "^9.1.0",
    "electron-prebuilt": "^1.4.13"
  }
}
